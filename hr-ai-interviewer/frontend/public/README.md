# public/

Vite serves everything in this folder as-is from the site root.

Drop the real Little Nap Recliners logo here as `logo.png` — the header in
`src/App.jsx` already references it as `/logo.png`. No code change needed,
just add the file (square image works best; it's displayed at 34x34).
